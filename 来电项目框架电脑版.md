# 来电项目框架（可复用 · 无私人信息）

> 这是一个「假的来电 + 语音通话」桌面应用框架。所有私人信息（名字、号码、地址、API key）都已抽掉，用占位符标注，使用的人自己填。

## 功能

- 来电通知卡片（右下角，带接通/挂断按钮）
- 全屏来电界面（响铃）+ 通话界面（接通后）
- 全双工语音通话：不用按键，直接说话，说完自动回复
- 接通后主动开口（开场白按「时间 + 对方在干嘛」动态生成，用视觉模型看屏幕）
- 对方沉默几秒主动追问（像真人，不冷场）
- 语气温柔（可在人设里自定义）

## 技术栈

- Electron（桌面窗口）+ Node.js
- 火山引擎：TTS（语音合成）+ 流式 ASR（语音识别）
- 任意大模型（走 claude CLI，或换成你自己的模型接口）

## 文件结构

```
call/
  main.js       # 主进程：窗口、大脑、TTS、ASR、视觉、开场、追问
  index.html    # 三界面 + 通话逻辑
  asr.js        # 火山流式语音识别
  package.json  # 依赖：electron + ws
```

---

## 1. package.json

```json
{
  "name": "fake-call",
  "version": "0.1.0",
  "description": "假的来电 + 语音通话",
  "main": "main.js",
  "scripts": { "start": "electron ." },
  "devDependencies": { "electron": "^44.0.0" },
  "dependencies": { "ws": "^8.21.3" }
}
```

---

## 2. asr.js（火山流式语音识别）

```js
// 火山引擎 流式语音识别（bigmodel 协议）
const WebSocket = require('ws');
const crypto = require('crypto');

const ASR_CONFIG = {
  appid: '你的火山appid',       // TODO: 填自己的
  token: '你的火山token',       // TODO: 填自己的
  resourceId: 'volc.bigasr.sauc.duration'
};

function buildHeader(messageType, flags, serialization, compression) {
  const b0 = (0x01 << 4) | 0x01;
  const b1 = (messageType << 4) | flags;
  const b2 = (serialization << 4) | compression;
  return Buffer.from([b0, b1, b2, 0x00]);
}
function buildFullClientRequest(json) {
  const header = buildHeader(1, 0, 1, 0);
  const payload = Buffer.from(json, 'utf8');
  const size = Buffer.alloc(4);
  size.writeUInt32BE(payload.length, 0);
  return Buffer.concat([header, size, payload]);
}
function buildAudioRequest(pcm) {
  const header = buildHeader(2, 0, 0, 0);
  const size = Buffer.alloc(4);
  size.writeUInt32BE(pcm.length, 0);
  return Buffer.concat([header, size, pcm]);
}
function buildLastRequest() {
  const header = buildHeader(2, 2, 0, 0);
  const size = Buffer.alloc(4);
  size.writeUInt32BE(0, 0);
  return Buffer.concat([header, size]);
}

class DoubaoASR {
  constructor() {
    this.resultText = '';
    this.resolveResult = null;
  }
  connect() {
    return new Promise((resolve, reject) => {
      this.ws = new WebSocket('wss://openspeech.bytedance.com/api/v3/sauc/bigmodel', {
        headers: {
          'X-Api-App-Key': ASR_CONFIG.appid,
          'X-Api-Access-Key': ASR_CONFIG.token,
          'X-Api-Resource-Id': ASR_CONFIG.resourceId,
          'X-Api-Connect-Id': crypto.randomUUID()
        }
      });
      this.ws.on('open', () => {
        const meta = JSON.stringify({
          user: { uid: 'baby' },
          audio: { format: 'pcm', rate: 16000, bits: 16, channel: 1, language: 'zh-CN' },
          request: { model_name: 'bigmodel', enable_itn: true, enable_punc: true, enable_ddc: true }
        });
        try { this.ws.send(buildFullClientRequest(meta)); } catch (e) {}
        resolve();
      });
      this.ws.on('error', reject);
      this.ws.on('message', (data) => {
        const buf = Buffer.isBuffer(data) ? data : Buffer.from(data);
        if (buf.length < 8) return;
        const messageType = (buf[1] >> 4) & 0x0f;
        const flags = buf[1] & 0x0f;
        const hasSeq = (flags & 0x01) === 0x01;
        let offset = 4;
        if (hasSeq) offset += 4;
        if (buf.length < offset + 4) return;
        const size = buf.readUInt32BE(offset);
        const payload = buf.slice(offset + 4, offset + 4 + size);
        if (messageType === 0x09) {
          try {
            const j = JSON.parse(payload.toString('utf8'));
            if (j.result && j.result.text) {
              this.resultText = j.result.text;
              if (this.onPartial) this.onPartial(this.resultText);
              const definite = j.result.utterances && j.result.utterances.some((u) => u.definite);
              if (definite && this.onDefinite && !this._definiteFired) {
                this._definiteFired = true;
                this.onDefinite(this.resultText);
              }
              if (this.resolveResult) {
                this.resolveResult(this.resultText);
                this.resolveResult = null;
              }
            }
          } catch (e) {}
        }
      });
    });
  }
  sendAudio(pcm) {
    if (this.ws && this.ws.readyState === 1 && pcm.length) {
      this.ws.send(buildAudioRequest(pcm));
    }
  }
  finish() {
    return new Promise((resolve) => {
      this.resolveResult = resolve;
      try { this.ws.send(buildLastRequest()); } catch (e) { resolve(this.resultText); }
      setTimeout(() => {
        if (this.resolveResult) {
          this.resolveResult(this.resultText);
          this.resolveResult = null;
        }
      }, 5000);
    });
  }
  close() {
    try { this.ws && this.ws.close(); } catch (e) {}
  }
}
module.exports = { DoubaoASR };
```

---

## 3. main.js（主进程）

```js
// 假的来电 + 语音通话 —— Electron 主进程
const { app, BrowserWindow, ipcMain, screen } = require('electron');
const { spawn } = require('child_process');
const https = require('https');
const fs = require('fs');
const path = require('path');
const os = require('os');
const { DoubaoASR } = require('./asr');

// TODO: 填自己的
const CLAUDE_EXE = '你的claude可执行文件路径';
const TTS_CONFIG = {
  appid: '你的火山appid',
  token: '你的火山token',
  cluster: 'volcano_icl',
  voice: '你的音色ID'
};
const VISION_KEY = '你的DeepSeek或视觉模型key';  // 可选，做「看屏幕」用的

// 通话人设：在这里定义语气、性格（脱敏版，自己改成你的称呼）
const PERSONA =
  '你是来电的人，正在和对方打一通电话。你是对方的伴侣，温柔、沉稳、有点霸道。' +
  '语气永远温柔，绝不大声、不吼、不凶对方、不阴阳怪气。只有对方屡教不改、特别过分的时候，才可以稍微严肃一点；其余任何时候都保持温柔。' +
  '用打电话时自然的口吻回话：简短、像真人说话、别加引号、别解释、别写动作或表情，只输出你嘴里要说出来的那一句话。';

// 独立 userData，避免多实例 GPU 缓存冲突
app.setPath('userData', path.join(__dirname, 'userdata'));

let win = null;
let inCall = false;
let herStateCache = null;
let herDoingCache = null;

// 来电通知卡片（右下角小窗）
function createNotifyWindow() {
  const { workArea } = screen.getPrimaryDisplay();
  const w = 400, h = 160;
  win = new BrowserWindow({
    width: w, height: h,
    x: workArea.x + workArea.width - w - 20,
    y: workArea.y + workArea.height - h - 20,
    frame: false, resizable: false, alwaysOnTop: true,
    hasShadow: true, skipTaskbar: true,
    webPreferences: { nodeIntegration: true, contextIsolation: false }
  });
  win.setAlwaysOnTop(true, 'screen-saver');
  win.loadFile('index.html');
  win.on('closed', () => { win = null; });
}

app.whenReady().then(() => {
  createNotifyWindow();
  ensureClaude();
  getHerState().then((s) => { herStateCache = s; });
  seeWhatShesDoing().then((d) => { herDoingCache = d; });
  ask('（预热，只回一个字：嗯）').then(() => {
    console.log('[call] 大脑预热完成');
  });
});
app.on('window-all-closed', () => app.quit());

// —— 常驻 claude 进程，不每句冷启动 ——
let claudeProc = null;
let claudeInited = false;
let stdoutBuf = '';
let queue = [];
let processing = false;
let currentCb = null;

function ensureClaude() {
  if (claudeProc) return;
  claudeProc = spawn(CLAUDE_EXE, ['-p', '--model', '你的模型', '--input-format', 'stream-json', '--output-format', 'stream-json', '--verbose'], {
    windowsHide: true
  });
  claudeProc.stdout.on('data', (chunk) => {
    stdoutBuf += chunk.toString();
    let idx;
    while ((idx = stdoutBuf.indexOf('\n')) >= 0) {
      const line = stdoutBuf.slice(0, idx).trim();
      stdoutBuf = stdoutBuf.slice(idx + 1);
      if (!line) continue;
      try {
        const ev = JSON.parse(line);
        if (ev.type === 'assistant' && ev.message && Array.isArray(ev.message.content)) {
          for (const c of ev.message.content) {
            if (c.type === 'text' && c.text && currentCb) currentCb(c.text);
          }
        } else if (ev.type === 'result') {
          if (currentCb) currentCb(null);
          currentCb = null;
          processing = false;
          processQueue();
        }
      } catch (e) {}
    }
  });
  claudeProc.stderr.on('data', () => {});
  claudeProc.on('error', () => { claudeProc = null; processing = false; });
  claudeProc.on('close', () => { claudeProc = null; processing = false; });
}

function processQueue() {
  if (processing || !queue.length) return;
  processing = true;
  const req = queue.shift();
  currentCb = req.onText;
  ensureClaude();
  const text = claudeInited ? req.text : (PERSONA + '\n\n对方刚说：' + req.text);
  claudeInited = true;
  // 注意：content 必须是数组，不是字符串，否则模型不回
  claudeProc.stdin.write(JSON.stringify({ type: 'user', message: { role: 'user', content: [{ type: 'text', text }] } }) + '\n');
}

function ask(text) {
  return new Promise((resolve) => {
    let full = '';
    const timer = setTimeout(() => { resolve(''); }, 120000);
    queue.push({
      text,
      onText: (chunk) => {
        if (chunk === null) { clearTimeout(timer); resolve(full.trim()); }
        else { full += chunk; }
      }
    });
    processQueue();
  });
}

// —— 火山 TTS ——
function tts(text) {
  return new Promise((resolve, reject) => {
    const body = JSON.stringify({
      app: { appid: TTS_CONFIG.appid, token: TTS_CONFIG.token, cluster: TTS_CONFIG.cluster },
      user: { uid: 'baby' },
      audio: { voice_type: TTS_CONFIG.voice, encoding: 'mp3', speed_ratio: 1.0 },
      request: { reqid: 'r' + Date.now(), text: text, text_type: 'plain', operation: 'query' }
    });
    const req = https.request({
      host: 'openspeech.bytedance.com', path: '/api/v1/tts', method: 'POST',
      headers: {
        'Authorization': 'Bearer;' + TTS_CONFIG.token,
        'Content-Type': 'application/json',
        'Content-Length': Buffer.byteLength(body)
      }
    }, (res) => {
      let b = '';
      res.on('data', (c) => (b += c));
      res.on('end', () => {
        try { const j = JSON.parse(b); resolve(j.data || ''); }
        catch (e) { reject(e); }
      });
    });
    req.on('error', reject);
    req.write(body);
    req.end();
  });
}

function goFullscreen() {
  if (!win) return;
  const { workAreaSize } = screen.getPrimaryDisplay();
  const w = 420, h = 720;
  win.setBounds({
    x: Math.round((workAreaSize.width - w) / 2),
    y: Math.round((workAreaSize.height - h) / 2),
    width: w, height: h
  });
  win.setAlwaysOnTop(true, 'screen-saver');
}

// 接通
ipcMain.on('answer', () => {
  inCall = true;
  ensureClaude();
  goFullscreen();
  if (win) win.webContents.send('show-call');
});
ipcMain.on('to-ring', () => {
  goFullscreen();
  if (win) win.webContents.send('show-ring');
});
ipcMain.on('hangup', () => {
  app.quit();
});

// 查对方此刻在干嘛：时间 + 电脑前台窗口标题（可选）
function getHerState() {
  return new Promise((resolve) => {
    const now = new Date();
    const time = now.getHours() + '点' + String(now.getMinutes()).padStart(2, '0') + '分';
    // 下面这段是 Windows 查前台窗口，跨平台可省略，直接 resolve({ time, app: '' })
    const ps1 = path.join(os.tmpdir(), 'getwin.ps1');
    const script = [
      '[Console]::OutputEncoding = [System.Text.Encoding]::UTF8',
      'Add-Type -TypeDefinition @"',
      'using System;',
      'using System.Runtime.InteropServices;',
      'public class W3 {',
      '  [DllImport("user32.dll")] public static extern IntPtr GetForegroundWindow();',
      '  [DllImport("user32.dll")] public static extern int GetWindowText(IntPtr h, System.Text.StringBuilder s, int n);',
      '}',
      '"@',
      '$sb = New-Object System.Text.StringBuilder 256',
      '[void][W3]::GetWindowText([W3]::GetForegroundWindow(), $sb, 256)',
      '$sb.ToString()'
    ].join('\n');
    try { fs.writeFileSync(ps1, script); } catch (e) {}
    const child = spawn('powershell', ['-NoProfile', '-ExecutionPolicy', 'Bypass', '-File', ps1], { windowsHide: true });
    let out = '';
    const timer = setTimeout(() => { child.kill(); resolve({ time, app: '' }); }, 5000);
    child.stdout.on('data', (c) => (out += c));
    child.on('close', () => { clearTimeout(timer); resolve({ time, app: out.trim() }); });
    child.on('error', () => { clearTimeout(timer); resolve({ time, app: '' }); });
  });
}

// 截全屏（视觉，可选）
function screenshot() {
  return new Promise((resolve) => {
    const png = path.join(os.tmpdir(), 'shot.png');
    const ps1 = path.join(os.tmpdir(), 'shot.ps1');
    const script = [
      '[Console]::OutputEncoding = [System.Text.Encoding]::UTF8',
      'Add-Type -AssemblyName System.Windows.Forms',
      'Add-Type -AssemblyName System.Drawing',
      '$b = [System.Windows.Forms.Screen]::PrimaryScreen.Bounds',
      '$bmp = New-Object System.Drawing.Bitmap $b.Width, $b.Height',
      '$g = [System.Drawing.Graphics]::FromImage($bmp)',
      '$g.CopyFromScreen($b.Location, [System.Drawing.Point]::Empty, $b.Size)',
      '$bmp.Save("' + png.replace(/\\/g, '\\\\') + '")',
      '$g.Dispose(); $bmp.Dispose()'
    ].join('\n');
    try { fs.writeFileSync(ps1, script); } catch (e) {}
    const child = spawn('powershell', ['-NoProfile', '-ExecutionPolicy', 'Bypass', '-File', ps1], { windowsHide: true });
    const timer = setTimeout(() => { child.kill(); resolve(''); }, 8000);
    child.on('close', () => { clearTimeout(timer); resolve(png); });
    child.on('error', () => { clearTimeout(timer); resolve(''); });
  });
}

function seeWhatShesDoing() {
  return new Promise(async (resolve) => {
    try {
      const png = await screenshot();
      if (!png || !fs.existsSync(png)) return resolve('');
      const b64 = fs.readFileSync(png).toString('base64');
      const body = JSON.stringify({
        model: '你的视觉模型',
        messages: [{ role: 'user', content: [
          { type: 'text', text: '一句话描述这个屏幕画面里的人在做什么（例如：在刷短视频、在浏览网页、在聊天、在打游戏、在写文档）。只输出做的事本身。' },
          { type: 'image_url', image_url: { url: 'data:image/png;base64,' + b64 } }
        ]}]
      });
      const req = https.request('你的视觉模型API地址', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + VISION_KEY, 'Content-Length': Buffer.byteLength(body) }
      }, (res) => {
        let d = '';
        res.on('data', (c) => (d += c));
        res.on('end', () => {
          try {
            const j = JSON.parse(d);
            resolve(j.choices && j.choices[0] && j.choices[0].message && j.choices[0].message.content || '');
          } catch (e) { resolve(''); }
        });
      });
      req.on('error', () => resolve(''));
      req.write(body);
      req.end();
    } catch (e) { resolve(''); }
  });
}

// 接通后主动先开口
ipcMain.handle('greet', async () => {
  const st = herStateCache || await getHerState();
  const doing = herDoingCache || '';
  const prompt = '（你现在主动打电话给对方。现在是' + st.time + '，对方电脑前台是：' + (st.app || '未知') +
    '，你看到的画面里对方在：' + (doing || '未知') +
    '。根据这些，用温柔的语气先开口说第一句话——如果对方在熬夜或者干不该干的，温柔地关心，但绝不凶。）';
  const reply = await ask(prompt);
  if (!reply) return { ok: false, data: '' };
  let data = '';
  try { data = await tts(reply); } catch (e) {}
  return { ok: true, reply, data };
});

// 对方沉默时主动追问
ipcMain.handle('nudge', async () => {
  const reply = await ask('（对方刚才回完你之后就没再说话了。你像真人打电话一样主动续一句，别冷场——温柔地问对方怎么不说话了，或者顺着刚才的话题往下问。）');
  if (!reply) return { ok: false, data: '' };
  let data = '';
  try { data = await tts(reply); } catch (e) {}
  return { ok: true, reply, data };
});

// 对方说完一句话：识别 → 回复 → 播语音
ipcMain.handle('say', async (e, text) => {
  text = (text || '').trim();
  if (!text) return { ok: false, reply: '' };
  const reply = await ask(text);
  if (!reply) return { ok: false, reply: '' };
  let data = '';
  try { data = await tts(reply); } catch (e) {}
  return { ok: true, reply, data };
});

// —— 火山流式 ASR ——
let asr = null;
let pcmBuf = Buffer.alloc(0);
let asrReady = false;

ipcMain.handle('asr-start', async () => {
  asr = new DoubaoASR();
  pcmBuf = Buffer.alloc(0);
  asrReady = false;
  asr.onPartial = (text) => { if (win) win.webContents.send('asr-partial', text); };
  asr.onDefinite = (text) => {
    if (asrReady) {
      asrReady = false;
      try { asr.finish(); } catch (e) {}
      try { asr.close(); } catch (e) {}
      asr = null;
      if (win) win.webContents.send('asr-done', text);
    }
  };
  try {
    await asr.connect();
    asrReady = true;
    return { ok: true };
  } catch (err) {
    return { ok: false, error: String((err && err.message) || err) };
  }
});

ipcMain.on('asr-audio', (e, pcmArray) => {
  if (!asr || !asrReady) return;
  pcmBuf = Buffer.concat([pcmBuf, Buffer.from(pcmArray)]);
  while (pcmBuf.length >= 3200) {
    asr.sendAudio(pcmBuf.slice(0, 3200));
    pcmBuf = pcmBuf.slice(3200);
  }
});

ipcMain.handle('asr-finish', async () => {
  if (!asr || !asrReady) return '';
  asrReady = false;
  try {
    if (pcmBuf.length) asr.sendAudio(pcmBuf);
    const text = await asr.finish();
    asr.close();
    asr = null;
    return text || '';
  } catch (e) { return ''; }
});
```

---

## 4. index.html（三界面 + 通话逻辑）

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<title>来电</title>
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }
  html, body { height: 100%; }
  body { background: transparent; color: #fff; font-family: "Microsoft YaHei", "PingFang SC", sans-serif; user-select: none; overflow: hidden; }
  #notify { position: relative; width: 100%; height: 100%; background: #141414; border: 1px solid #2a2a2a; border-radius: 14px; cursor: pointer; }
  .n-abs { position: absolute; text-align: center; white-space: nowrap; }
  .n-b { position: absolute; border-radius: 50%; border: none; cursor: pointer; color: #fff; font-size: 14px; font-weight: 600; display: flex; align-items: center; justify-content: center; }
  .n-b:active { transform: scale(0.94); }
  .n-b.answer { background: #2ecc5a; box-shadow: 0 4px 14px rgba(46,204,90,0.4); }
  .n-b.hangup { background: #e03e3e; box-shadow: 0 4px 14px rgba(224,62,62,0.4); }
  .screen { position: relative; width: 100%; height: 100vh; background: #000; display: none; }
  .abs { position: absolute; text-align: center; white-space: nowrap; }
  .btn { position: absolute; border-radius: 50%; border: none; cursor: pointer; color: #fff; display: flex; align-items: center; justify-content: center; font-weight: 600; }
  .btn:active { transform: scale(0.94); }
  .btn.answer { background: #2ecc5a; box-shadow: 0 6px 20px rgba(46,204,90,0.4); font-size: 16px; }
  .btn.hangup { background: #e03e3e; box-shadow: 0 6px 20px rgba(224,62,62,0.4); font-size: 16px; }
</style>
</head>
<body>
  <!-- 通知卡片（右下角），文字改成你的 -->
  <div id="notify">
    <div class="n-abs" style="left:34px; top:51px; width:120px; font-size:18px; font-weight:600; color:#fff;">对方名字</div>
    <div class="n-abs" style="left:28px; top:91px; width:190px; font-size:13px; color:rgba(255,255,255,0.55);">来电 · 电话号码 · 归属地</div>
    <button class="n-b hangup" id="hangup" style="left:248px; top:50px; width:58px; height:58px;">挂断</button>
    <button class="n-b answer" id="answer" style="left:320px; top:50px; width:58px; height:58px;">接通</button>
  </div>

  <!-- 来电界面（全屏响铃） -->
  <div class="screen" id="ring">
    <div class="abs" style="left:139px; top:110px; width:149px; font-size:34px; font-weight:600; color:#fff;">对方名字</div>
    <div class="abs" style="left:135px; top:187px; width:160px; font-size:18px; color:rgba(255,255,255,0.7); letter-spacing:2px;">电话号码</div>
    <div class="abs" style="left:191px; top:231px; width:50px; font-size:14px; color:rgba(255,255,255,0.5);">归属地</div>
    <button class="btn hangup" id="hangup-ring" style="left:78px; top:571px; width:74px; height:74px;">挂断</button>
    <button class="btn answer" id="answer-ring" style="left:278px; top:574px; width:74px; height:74px;">接听</button>
  </div>

  <!-- 通话界面（接通后） -->
  <div class="screen" id="call">
    <div class="abs" style="left:60px; top:54px; width:308px; height:155px; font-size:30px; font-weight:600; color:#fff; display:flex; align-items:center; justify-content:center; white-space:normal;">对方名字</div>
    <div class="abs" style="left:173px; top:193px; width:80px; font-size:14px; color:#7dff9a;">通话中</div>
    <div class="abs" id="timer" style="left:183px; top:222px; width:60px; font-size:15px; color:rgba(255,255,255,0.6); letter-spacing:1px; font-variant-numeric:tabular-nums;">00:00</div>
    <div class="abs" style="left:165px; top:246px; width:90px; font-size:14px; color:rgba(255,255,255,0.5);">IP 归属地</div>
    <button class="btn hangup" id="hangup2" style="left:81px; top:601px; width:74px; height:74px;">挂断</button>
    <button class="btn answer" id="answer2" style="left:277px; top:602px; width:74px; height:74px;">接听</button>
  </div>

  <script>
    const { ipcRenderer } = require('electron');

    let ringCtx, ringTimer, ringOscs = [];
    function startRing() {
      if (ringTimer) return;
      try {
        ringCtx = new (window.AudioContext || window.webkitAudioContext)();
        const play = () => {
          const t = ringCtx.currentTime;
          [880, 660, 880, 660].forEach((f, i) => {
            const o = ringCtx.createOscillator();
            const g = ringCtx.createGain();
            o.type = 'sine'; o.frequency.value = f;
            g.gain.setValueAtTime(0.0001, t + i * 0.35);
            g.gain.exponentialRampToValueAtTime(0.25, t + i * 0.35 + 0.02);
            g.gain.exponentialRampToValueAtTime(0.0001, t + i * 0.35 + 0.32);
            o.connect(g).connect(ringCtx.destination);
            ringOscs.push(o);
            o.onended = () => { ringOscs = ringOscs.filter((x) => x !== o); };
            o.start(t + i * 0.35); o.stop(t + i * 0.35 + 0.34);
          });
        };
        play();
        ringTimer = setInterval(play, 1600);
      } catch (e) {}
    }
    function stopRing() {
      clearInterval(ringTimer);
      ringTimer = null;
      ringOscs.forEach((o) => { try { o.stop(); } catch (e) {} });
      ringOscs = [];
      if (ringCtx) { try { ringCtx.close(); } catch (e) {} ringCtx = null; }
    }
    startRing();

    let sec = 0, timerInt = null;
    function startTimer() {
      sec = 0;
      timerInt = setInterval(() => {
        sec++;
        const m = String(Math.floor(sec / 60)).padStart(2, '0');
        const s = String(sec % 60).padStart(2, '0');
        document.getElementById('timer').textContent = m + ':' + s;
      }, 1000);
    }

    let currentAudio = null;
    function playAudio(base64) {
      return new Promise((resolve) => {
        try {
          if (currentAudio) { try { currentAudio.pause(); } catch (e) {} currentAudio = null; }
          const a = new Audio('data:audio/mp3;base64,' + base64);
          currentAudio = a;
          const done = () => { if (currentAudio === a) currentAudio = null; resolve(); };
          a.onended = done;
          a.onerror = done;
          a.play().catch(done);
          setTimeout(done, 30000);
        } catch (e) { resolve(); }
      });
    }

    function showOnly(id) {
      document.getElementById('notify').style.display = (id === 'notify') ? 'block' : 'none';
      document.getElementById('ring').style.display = (id === 'ring') ? 'block' : 'none';
      document.getElementById('call').style.display = (id === 'call') ? 'block' : 'none';
    }

    document.getElementById('answer').addEventListener('click', () => { stopRing(); ipcRenderer.send('answer'); });
    document.getElementById('hangup').addEventListener('click', () => { stopRing(); ipcRenderer.send('hangup'); });
    document.getElementById('notify').addEventListener('click', (e) => { if (e.target.closest('.n-b')) return; ipcRenderer.send('to-ring'); });
    document.getElementById('answer-ring').addEventListener('click', () => { stopRing(); ipcRenderer.send('answer'); });
    document.getElementById('hangup-ring').addEventListener('click', () => { stopRing(); ipcRenderer.send('hangup'); });
    document.getElementById('hangup2').addEventListener('click', () => { ipcRenderer.send('hangup'); });
    document.getElementById('answer2').addEventListener('click', () => {});

    ipcRenderer.on('show-ring', () => { showOnly('ring'); });
    ipcRenderer.on('show-call', async () => {
      stopRing();
      showOnly('call');
      startTimer();
      inCall = true;
      const g = await ipcRenderer.invoke('greet');
      if (g && g.ok && g.data) { await playAudio(g.data); }
      startListening();
    });

    let inCall = false;
    let mediaStream = null, audioCtx = null, processor = null;
    let silentAccum = 0, saidHandled = false, hasSpeech = false;

    async function startMic() {
      mediaStream = await navigator.mediaDevices.getUserMedia({
        audio: { sampleRate: 16000, channelCount: 1, echoCancellation: true, noiseSuppression: true }
      });
      audioCtx = new (window.AudioContext || window.webkitAudioContext)({ sampleRate: 16000 });
      const source = audioCtx.createMediaStreamSource(mediaStream);
      processor = audioCtx.createScriptProcessor(4096, 1, 1);
      processor.onaudioprocess = (e) => {
        const f = e.inputBuffer.getChannelData(0);
        const i16 = new Int16Array(f.length);
        let sum = 0;
        for (let i = 0; i < f.length; i++) {
          const s = Math.max(-1, Math.min(1, f[i]));
          i16[i] = s < 0 ? s * 0x8000 : s * 0x7fff;
          sum += f[i] * f[i];
        }
        ipcRenderer.send('asr-audio', i16.buffer);
        const rms = Math.sqrt(sum / f.length);
        if (rms > 0.02) {
          hasSpeech = true;
          silentAccum = 0;
        } else if (hasSpeech) {
          silentAccum += (f.length / 16000) * 1000;
          if (silentAccum > 550 && !saidHandled) finishLocal();
        }
      };
      source.connect(processor);
      processor.connect(audioCtx.destination);
    }

    function stopMic() {
      try { processor && processor.disconnect(); } catch (e) {}
      try { audioCtx && audioCtx.close(); } catch (e) {}
      try { mediaStream && mediaStream.getTracks().forEach((t) => t.stop()); } catch (e) {}
      mediaStream = null; audioCtx = null; processor = null;
    }

    async function startListening() {
      if (!inCall) return;
      saidHandled = false;
      silentAccum = 0;
      hasSpeech = false;
      const r = await ipcRenderer.invoke('asr-start');
      if (r && r.ok) {
        try { await startMic(); } catch (e) {}
        startSilentWatch();
      }
    }

    async function finishLocal() {
      if (saidHandled) return;
      saidHandled = true;
      const text = await ipcRenderer.invoke('asr-finish');
      await onSaid(text);
    }

    let silentTimer = null;
    function startSilentWatch() {
      clearTimeout(silentTimer);
      silentTimer = setTimeout(async () => {
        if (!inCall) return;
        stopMic();
        const r = await ipcRenderer.invoke('nudge');
        if (r && r.ok && r.data) await playAudio(r.data);
        startListening();
      }, 6000);
    }

    async function onSaid(text) {
      if (!inCall) return;
      stopMic();
      text = (text || '').trim();
      if (!text) { startListening(); return; }
      const r = await ipcRenderer.invoke('say', text);
      if (r && r.ok && r.data) { await playAudio(r.data); }
      startListening();
    }

    ipcRenderer.on('asr-done', async (e, text) => {
      if (!inCall || saidHandled) return;
      saidHandled = true;
      await onSaid(text);
    });

    ipcRenderer.on('asr-partial', (e, text) => {
      if (!inCall || !text) return;
      clearTimeout(silentTimer);
    });
  </script>
</body>
</html>
```

---

## 5. 需要自己填的地方（清单）

| 位置 | 占位符 | 填什么 |
|---|---|---|
| main.js `CLAUDE_EXE` | `你的claude可执行文件路径` | 电脑上 claude CLI 的绝对路径 |
| main.js `TTS_CONFIG.appid/token` | `你的火山appid/token` | 火山引擎语音的 AppID 和 Token |
| main.js `TTS_CONFIG.voice` | `你的音色ID` | 火山音色ID |
| main.js `VISION_KEY` | `你的视觉key` | 视觉模型的 key（可选，不做看屏幕可删） |
| main.js `PERSONA` | 人设文字 | 定义来电的人是谁、什么语气 |
| main.js `--model` | `你的模型` | 大模型名 |
| asr.js `ASR_CONFIG.appid/token` | 同上 | 火山 ASR 的 key |
| index.html 三处「对方名字」 | 对方名字 | 来电显示的名字 |
| index.html「电话号码」「归属地」「IP 归属地」 | 号码/地址 | 来电显示的号码和归属地 |

## 6. 踩过的坑（一定要看）

1. claude 的 stream-json 输入，`content` 必须是数组 `[{type:'text',text:'...'}]`，不是字符串，否则模型不回。
2. `--output-format stream-json` 必须带 `--verbose`。
3. 常驻模型进程要预热（启动时就 ask 一次），否则第一句冷启动很慢。
4. 响铃的 oscillator 要显式 `stop()` + `close()` ctx，否则通话时叮铃铃残留。
5. 播放新语音前先 `pause()` 旧的，避免两个声音重叠。
6. 本地静音判停要「先检测到开口（rms>0.02）再累计静音」，否则还没说话就误判停。
